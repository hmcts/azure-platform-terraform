---

gateways:
  - gateway_configuration:
      host_name_suffix: service.core-compute-ethosldata.internal
      ssl_host_name_suffix: platform.hmcts.net
      certificate_name: wildcard-platform-hmcts-net
    app_configuration:
    # RPE
      - product: rpe
        component: service-auth-provider-ethosldata

    # CCD
      - product: ccd
        component: data-store-api-ethosldata
      - product: ccd
        component: definition-store-api-ethosldata
      - product: ccd
        component: user-profile-api-ethosldata
      - product: ccd
        component: case-activity-api-ethosldata
      - product: ccd
        component: admin-web-ethosldata
        ssl_enabled: true

    # SIDAM
      - product: idam
        component: api-ethosldata
        cookie_based_affinity: Enabled
        ssl_enabled: true
      - product: idam
        component: web-admin-ethosldata
        cookie_based_affinity: Enabled
        ssl_enabled: true

